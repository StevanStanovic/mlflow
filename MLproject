name: MLProject

conda_env: conda.yaml

entry_points:
  ann:
    parameters:
      nb_batch : {type: int, default: 32}
      nb_epochs : {type: int, default: 100}
    command:
      "python ANN.py {nb_batch} {nb_epochs}"
      
  decisionTree:
    parameters:
      criterion : {type: str, default: "entropy"}
    command:
      "python Decision_Tree.py {criterion}"
      
  gaussianNaiveBayes:
    command:
      "python Gaussian_Naive_Bayes.py"
      
  knn:
    parameters:
      nb_neighbors : {type: int, default: 2}
      metric : {type: str, default: "minkowski"}
      p : {type: int, default: 2}
    command:
      "python KNN.py {nb_neighbors} {metric} {p}"
  
  randomForest:
    parameters:
      nb_estim : {type: int, default: 10}
      criterion : {type: str, default: "entropy"}
    command:
      "python Random_Forest.py {nb_estim} {criterion}"
      
  regressionLogistique:
    command:
      "python Régression_Logistique.py"
  
  SVMGauss:
    parameters:
      C : {type: float, default: 1.0}
      kernel : {type: str, default: "rbf"}
    command:
      "python SVM_Gauss.py {C} {kernel}"
      
  main:
    command: "python test.py"
